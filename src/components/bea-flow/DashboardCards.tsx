export function DashboardCards({ title, items }:{ title:string; items:Array<{label:string; value:string|number; href?:string}> }){ return <section style={{display:'grid',gap:14}}><h2 style={{margin:0,color:'#102a43'}}>{title}</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(210px,1fr))',gap:14}}>{items.map(item=><a key={item.label} href={item.href ?? '#'} style={{background:'white',border:'3px solid #102a43',borderRadius:20,padding:18,color:'#102a43',textDecoration:'none',boxShadow:'6px 6px 0 rgba(16,42,67,.1)'}}><strong style={{display:'block',fontSize:28}}>{item.value}</strong><span>{item.label}</span></a>)}</div></section> }
