const express = require('express'); const router = express.Router(); router.get('/module/:moduleCode', async (req,res)=>res.json({moduleCode:req.params.moduleCode,note:'Return video_url, embed_url, captions, thumbnail, transcript and completion rule.'})); router.post('/progress', async (req,res)=>{const {learner_id,module_code,progress_ratio,last_position_seconds}=req.body; res.json({learner_id,module_code,progress_ratio,last_position_seconds,completed:Number(progress_ratio)>=0.85});}); module.exports = router;
