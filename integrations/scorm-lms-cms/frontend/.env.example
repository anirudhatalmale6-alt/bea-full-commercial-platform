# Frontend build-time config. Copy to .env for local dev if needed.
# In production the nginx container proxies /api to the backend service,
# so this is only used by `vite dev`.
VITE_API_URL=http://localhost:4000
